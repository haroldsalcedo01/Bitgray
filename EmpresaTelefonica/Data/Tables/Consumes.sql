CREATE TABLE Consumes
(
Id INT IDENTITY(1,1) PRIMARY KEY,
UserId INT FOREIGN KEY REFERENCES Users(Id),
Time  DECIMAL (18,2) NOT NULL,
DateCall DATETIME NOT NULL,
ValueCall DECIMAL(18,2) NOT NULL
)
